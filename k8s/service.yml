apiVersion: v1
kind: Service
metadata:
  name: havefun
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "havefun"
    port: 8080
  selector:
    app: havefun
